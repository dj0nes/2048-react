  Stage sequence:
  - 0D {x:1,y:1,z:1} — single 3D cube, camera starts at radius 0.8 (right on top of it) and zooms out dramatically over 3s to 3× normal
  viewing distance
  - 1D {x:4,y:1,z:1} — four cubes in a line, normal framing (no orbit, camera just holds)
  - 2D {x:3,y:3,z:1} — flat 3×3 grid, camera locked head-on. The 8 non-first-row tiles animate in from z=-8 (behind the screen)
  - 3D {x:3,y:3,z:3} — camera starts dead ahead (looks exactly like the 2D stage), then orbits after 1.5s — the hidden back layers
  emerge as depth is revealed
  - 4D×3/4/5 — genie entrance, loops forever
  
  